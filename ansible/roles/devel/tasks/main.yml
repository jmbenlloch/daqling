- name: Install packages
  yum:
    name: "{{ packages }}"
  vars:
    packages:
    - git
    - vim
    - wget
    - cmake3
    - mlocate
    - graphviz
    - python36-pip
    - centos-release-scl-rh
    - llvm-toolset-7

- name: Updatedb
  shell: updatedb
